# V1 archive

These files are the retired six-signal workflow. They are not decision logic for a current run.

- `sops/` holds the old master digest SOP and the six signal SOPs.
- `accounts-in-scope.md` is the old account table.
- `agents/` holds digest-orchestrator, digest-verifier, and signal-sop-runner.
- `templates/` holds the old run manifest and batch lane log.

The active procedure is the V2 SOPs in the repository `sops/` folder and `AGENTS.md`.
